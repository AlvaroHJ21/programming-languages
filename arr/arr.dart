void main(List<String> args) {
  
  //Declaración de una lista

  final arr = [1,2,3,4,];
  // List<int> arr = [1, 2, 3, 4, 5];
  print(arr);
  // print(arr);
  // print(arr[0]);
  // print(arr[1]);
  // print(arr[2]);
  // print(arr[3]);
  // print(arr[4]);
  // print(arr.length);
  // print(arr.isEmpty);
  // print(arr.isNotEmpty);
  // print(arr.reversed);
  // print(arr.reversed.toList());
  // print(arr.first);
  // print(arr.last);
  // print(arr.single);
  // print(arr.asMap());
  // print(arr.contains(1));
  // print(arr.indexOf(1));
  // print(arr.lastIndexOf(1));
  // print(arr.sublist(1));
  // print(arr.sublist(1, 3));
  // print(arr.skip(1));
  // print(arr.skipWhile((value) => value < 3));
  // print(arr.take(1));
  // print(arr.takeWhile((value) => value < 3));
  // print(arr.map((value) => value * 2));
  // print(arr.where((value) => value < 3));
  // print(arr.expand((value) => [value, value]));
  // print(arr.reduce((value, element) => value + element));
  // print(arr.fold(0, (value, element) => value + element));
  // print(arr.every((value) => value < 3));
  // print(arr.any((value) => value < 3));
  // print(arr.toList());
  // print(arr.toSet());
  // print(arr.join(','));
  // print(arr.join());
}